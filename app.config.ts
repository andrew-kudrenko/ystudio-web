export default defineAppConfig({
  ui: {
    formField: {
      default: {
        container: "mt-1 relative w-full",
      },
    },
    input: {
      default: {
        input: "w-full",
      },
    },
    select: {
      default: {
        base: "w-full",
      },
    },
  },
});
