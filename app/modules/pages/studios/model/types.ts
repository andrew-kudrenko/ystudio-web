export interface CreateStudioFormValues {
  name: string;
  capacity: number;
  description: string;
  address: string;
}

export interface EditStudioFormValues {
  name: string;
  capacity: number;
  description: string;
  address: string;
}
